# Codex Compatibility Instructions

This repository uses `AGENT.md` as the canonical instruction file.

Read `AGENT.md` in this directory completely before changing code.

Work context (plans, status, history) lives in:

```text
vault/01 - Work/igreja/plataforma/
```

When a ticket exists, treat `tasks/<ID>/` in that vault folder as the working source of truth. Do not keep task state only in chat.
